name: Send Static Button
on: [push]

jobs:
  telegram_job:
    runs-on: ubuntu-latest
    steps:
      - name: Send One Time Message
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="💡 **مرحباً بكم في المنظومة التعليمية**%0Aاضغط على الزر بالأسفل للبدء والاستخدام الآمن." \
          -d parse_mode="Markdown" \
          -d reply_markup='{"inline_keyboard":[[{"text":"🚀 تنفيذ المنظومة","url":"https://attaandtakadom.github.io/atta/"}]]}'

name: Send Main Button
on: [push]

jobs:
  telegram_job:
    runs-on: ubuntu-latest
    steps:
      - name: Send Message with Button
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="💡 **مرحباً بكم في المنظومة التعليمية**%0A%0Aاضغط على الزر بالأسفل للدخول والاستخدام الآمن." \
          -d parse_mode="Markdown" \
          -d disable_web_page_preview=true \
          -d reply_markup='{"inline_keyboard":[[{"text":"🚀 تنفيذ المنظومة","url":"https://attaandtakadom.github.io/atta/"}]]}'

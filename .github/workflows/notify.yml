name: Secure Telegram Notification
on: [push]

jobs:
  telegram_job:
    runs-on: ubuntu-latest
    steps:
      - name: Send Clean Notification
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="📢 **تحديث المنظومة التعليمية**%0A%0Aتمت إضافة تحسينات جديدة بنجاح. يمكنك الآن المتابعة عبر الرابط الآمن أدناه." \
          -d parse_mode="Markdown" \
          -d disable_web_page_preview=true \
          -d reply_markup='{"inline_keyboard":[[{"text":"🔗 دخول المنظومة","url":"https://attaandtakadom.github.io/atta/"}]]}'
